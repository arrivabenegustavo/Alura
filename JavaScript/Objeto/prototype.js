// Uso no console do navegador

// Array
const arr = [1, 2 , 3];

// Acessa os métodos/propriedades de Array
arr.__proto__

// Objeto
const obj = {a: 1, b: 2};

// Acessa os métodos/propriedades do objeto
obj.__proto__

// É possível acessar tbm as propriedades de uma string
texto.__proto__